<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="CommonAppTheme">
        <!-- 应用的主要色调，actionBar默认使用该颜色，Toolbar导航栏的底色 -->
        <item name="colorPrimary">@color/color_fff</item>
        <!-- 应用的主要暗色调，statusBarColor默认使用该颜色 -->
        <item name="colorPrimaryDark">@color/color_fff</item>
        <!-- 应用的强调色，CheckBox，RadioButton，SwitchCompat等一般控件的选中效果默认采用该颜色 -->
        <item name="colorAccent">@color/color_ff6699</item>
        <!-- 窗口背景颜色 -->
        <item name="android:windowBackground">@color/color_fff</item>
        <!--Button、TextView的文字颜色-->
        <item name="android:textColor">@color/color_333</item>
        <!-- Button、TextView的提示文字颜色 -->
        <item name="android:textColorHint">@color/color_999</item>
        <!-- ActionMode覆盖Actionbar，不顶下来 -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- ActionMode的颜色 -->
        <item name="actionModeBackground">@color/color_fff</item>
        <!-- 解决 Android 5.1 及以上版本 Button 英文字符串自动变大写的问题 -->
        <item name="android:textAllCaps">false</item>
        <!-- 默认隐藏输入法，开启这个选项后会导致输入对话框在关闭之后不能关闭软键盘   -->
        <!--<item name="android:windowSoftInputMode">stateHidden</item>-->
        <!--处理默认 recycleview 等滑动空间滑动越界阴影-->
        <item name="android:overScrollMode">never</item>

    </style>

    <!--启动页-->
    <style name="SplashTheme" parent="CommonAppTheme">
        <item name="android:windowFullscreen">true</item>
        <!--                <item name="android:background">@mipmap/splash</item>-->
<!--        <item name="android:background">@android:color/transparent</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否禁用窗口的预览动画-->
        <item name="android:windowDisablePreview">false</item>
    </style>
</resources>
